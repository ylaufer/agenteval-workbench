version: 1
journeys:
  - name: simple_rag_query
    required_spans:
      - http.server:/ask
      - retriever.search
      - llm.generate
    forbidden_spans:
      - support_ticket.create
    required_order:
      - http.server:/ask
      - retriever.search
      - llm.generate
    max_span_count: 6
    max_total_duration_ms: 2500
