version: 1
journeys:
  - name: simple_rag_query
    required_spans:
      - http.server:/ask
      - retriever.search
      - llm.generate
    forbidden_spans:
      - support_ticket.create
    required_order:
      - http.server:/ask
      - retriever.search
      - llm.generate
    min_occurrences:
      retriever.search: 1
    max_occurrences:
      llm.generate: 2
    max_span_count: 6
    max_total_duration_ms: 2500
